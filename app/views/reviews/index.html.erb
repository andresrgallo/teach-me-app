# <div class="container">
    <% if @reviews.present? %>
        <%= render @reviews %>
    <% else %>
        <h2>You haven't reviewed any Profile yet. </h2>
    <% end %>
    <h2>Here are Profiles that all users have reviewed. </h2>
    <ul>
    <% Review.reviewed_profile.each do |t| %>
        <% if !t.nil? %>
            <li><%= link_to t.first_name, t %></li>
        <% end %>
    <% end %>
    </ul>


</div>