<div class="row">
		<% @categories.each do |c| %>
			<div class="col-md-3">
				<%= c.description %>
					<%#= image_tag play.play_img.url(:play_index), class: "play" %>
				</a>
			</div>
        <% end %>
</div>